export interface MinitripDialogData {
  tripId: number;  
  currentTripShift: string;
   name:string
}
export interface MinitripDialogDataReturn {
  driverId: number;  
  busId: string;
  name:string;
}